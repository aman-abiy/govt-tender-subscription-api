import Account from '../interfaces/Account';
import mongoose from 'mongoose';

export const verifyAccountSMS = (account: Account, passwordResetLink: URL) : string => {
    const sms: string = `sasdasd`;
    return sms
}